{
  "name": "business-partner-ai-functions",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "openai": "^5.0.0",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
